# 🚀 Быстрый старт OnePlace

## ✅ Что уже должно быть установлено

- ✅ Node.js 18+ (проверьте: `node --version`)
- ✅ PostgreSQL 15+ (проверьте наличие в меню Пуск)

---

## 📦 Шаг 1: Установка зависимостей проекта

Откройте PowerShell в папке проекта:

```powershell
# Перейдите в папку проекта (если еще не там)
cd C:\Users\venag\Downloads\oneplace

# Установите зависимости для frontend
npm install

# Установите зависимости для backend
cd backend
npm install
cd ..
```

---

## 🗄️ Шаг 2: Настройка базы данных PostgreSQL

### 2.1 Создайте базу данных

**Вариант А: Через pgAdmin (графический интерфейс)**

1. Откройте **pgAdmin 4** (найдите в меню Пуск)
2. При первом запуске может попросить мастер-пароль — установите свой или оставьте пустым
3. В левой панели найдите **Servers** → **PostgreSQL 15**
4. При подключении введите пароль для `postgres` (который указали при установке)
5. Правой кнопкой на **PostgreSQL 15** → **Create** → **Database...**
6. **Name**: `onepace`
7. Нажмите **Save**

**Вариант Б: Через командную строку (быстрее)**

```powershell
# Откройте psql (введите пароль postgres при запросе)
& "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres

# В psql выполните:
CREATE DATABASE onepace;
\q
```

### 2.2 Инициализируйте таблицы

```powershell
# Выполните скрипт инициализации
& "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres -d onepace -f "backend\init-database.sql"
```

Введите пароль `postgres` при запросе.

**Или через pgAdmin:**
1. Правой кнопкой на базе данных `onepace` → **Query Tool**
2. Откройте файл `backend\init-database.sql`
3. Скопируйте весь текст в Query Tool
4. Нажмите **F5** для выполнения

---

## ⚙️ Шаг 3: Настройка переменных окружения

```powershell
cd backend
copy env.example .env
```

**Откройте файл `backend\.env` в любом текстовом редакторе** и укажите:

```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=onepace
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ВАШ_ПАРОЛЬ_POSTGRES  # ← Пароль, который указали при установке PostgreSQL
```

**Важно:** Замените `ВАШ_ПАРОЛЬ_POSTGRES` на реальный пароль!

---

## 🚀 Шаг 4: Запуск проекта

### Запустите в двух терминалах:

**Терминал 1 - Backend:**
```powershell
cd backend
npm run dev
```

Должно появиться:
```
🚀 Server running on port 3002
💾 Database: localhost:5432/onepace
Database connected successfully: ...
```

**Терминал 2 - Frontend:**
```powershell
# В корне проекта (не в backend!)
npm run dev
```

Должно открыться: **http://localhost:3000**

---

## ✅ Проверка работы

1. Откройте http://localhost:3000
2. Нажмите "Регистрация"
3. Заполните форму и зарегистрируйтесь
4. Если регистрация прошла успешно — всё работает! 🎉

---

## 🐛 Если что-то не работает

### Ошибка: "Cannot connect to database"
- Проверьте, что PostgreSQL запущен (Диспетчер задач → Службы → postgresql)
- Проверьте пароль в `backend\.env`
- Убедитесь, что база данных `onepace` создана

### Ошибка: "Module not found"
```powershell
# Переустановите зависимости
npm install
cd backend
npm install
```

### Ошибка: "Port already in use"
- Закройте другие приложения, использующие порты 3000 или 3002
- Или измените порты в конфигурации

---

## 📋 Чеклист перед запуском

- [ ] Node.js установлен (`node --version`)
- [ ] PostgreSQL установлен (есть в меню Пуск)
- [ ] Зависимости установлены (`npm install` в корне и в `backend/`)
- [ ] База данных `onepace` создана
- [ ] Таблицы инициализированы (скрипт `init-database.sql` выполнен)
- [ ] Файл `backend\.env` создан и настроен
- [ ] PostgreSQL запущен (проверьте в Диспетчере задач → Службы)

---

Готово! Теперь проект должен работать 🚀


