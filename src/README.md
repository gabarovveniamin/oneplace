# Структура проекта

Этот проект организован по принципу Feature-Sliced Design (FSD) для обеспечения масштабируемости и поддерживаемости кода.

## 📁 Структура папок

```
src/
├── app/                    # Конфигурация приложения
├── pages/                  # Страницы приложения
├── widgets/                # Крупные UI блоки
├── features/               # Бизнес-функции
│   ├── jobs/              # Функциональность вакансий
│   │   ├── components/    # Компоненты вакансий
│   │   ├── hooks/         # Хуки для работы с вакансиями
│   │   ├── types/         # Типы для вакансий
│   │   └── constants/     # Константы для вакансий
│   ├── auth/              # Аутентификация
│   │   └── components/    # Компоненты авторизации
│   └── profile/           # Профиль пользователя
│       └── components/    # Компоненты профиля
├── entities/              # Бизнес-сущности
├── shared/                # Переиспользуемые ресурсы
│   ├── ui/               # UI компоненты
│   │   ├── components/   # Базовые компоненты
│   │   └── figma/        # Компоненты из Figma
│   ├── lib/              # Утилиты и библиотеки
│   ├── types/            # Общие типы
│   └── constants/        # Общие константы
└── core/                 # Ядро приложения
    ├── api/              # API слой
    ├── hooks/            # Общие хуки
    ├── contexts/         # React контексты
    └── utils/            # Утилиты
```

## 🎯 Принципы организации

### Features (Функции)
- **jobs** - вся логика работы с вакансиями
- **auth** - аутентификация и регистрация
- **profile** - профиль пользователя

### Shared (Общие ресурсы)
- **ui** - переиспользуемые UI компоненты
- **types** - общие типы данных
- **constants** - константы приложения

### Core (Ядро)
- **api** - работа с API
- **hooks** - общие хуки
- **contexts** - React контексты

## 🔧 Импорты

### Внутри feature
```typescript
import { Component } from './components/Component';
import { useHook } from './hooks/useHook';
```

### Между features
```typescript
import { Component } from '../shared/ui/components';
import { Type } from '../shared/types';
```

### Из shared
```typescript
import { Component } from './shared/ui/components';
import { Type } from './shared/types';
```

## 📋 Правила

1. **Изоляция** - features не должны зависеть друг от друга
2. **Переиспользование** - общий код выносится в shared
3. **Иерархия** - соблюдение правил импорта (только вниз по иерархии)
4. **Индексация** - каждый модуль имеет index.ts для экспорта

## 🚀 Преимущества

- **Масштабируемость** - легко добавлять новые функции
- **Поддерживаемость** - четкое разделение ответственности
- **Переиспользование** - общие компоненты в shared
- **Тестируемость** - изолированные модули
- **Командная работа** - разные разработчики могут работать над разными features
