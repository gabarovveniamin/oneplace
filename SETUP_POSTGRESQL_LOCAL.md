# 🐘 Настройка PostgreSQL локально (без Docker)

Инструкция по установке и настройке PostgreSQL на Windows 11 для проекта OnePlace.

## 📦 Шаг 1: Установка PostgreSQL

### Способ А: Официальный установщик (Рекомендуется)

1. **Скачайте PostgreSQL:**
   - Перейдите на https://www.postgresql.org/download/windows/
   - Или прямую ссылку: https://www.postgresql.org/download/windows/installer/
   - Скачайте **PostgreSQL 15** или **16** для Windows x86-64

2. **Запустите установщик:**
   - Дважды кликните на скачанный файл
   - Нажмите "Next" на всех экранах приветствия

3. **Важные настройки при установке:**
   - **Installation Directory**: Оставьте по умолчанию `C:\Program Files\PostgreSQL\15`
   - **Select Components**: Оставьте все компоненты (включая pgAdmin)
   - **Data Directory**: По умолчанию `C:\Program Files\PostgreSQL\15\data`
   - **Password**: Придумайте пароль для пользователя **postgres** (запомните его!)
     - ⚠️ **ВАЖНО**: Это будет мастер-пароль суперпользователя
     - Пример: `postgres123` (но лучше придумайте свой)
   - **Port**: Оставьте **5432** (стандартный порт)
   - **Advanced Options**: Оставьте по умолчанию
   - **Pre Installation Summary**: Проверьте настройки и нажмите "Next"
   - **Ready to Install**: Нажмите "Next" и дождитесь установки

4. **После установки:**
   - Снимите галочку "Launch Stack Builder" (не нужен для нашей задачи)
   - Нажмите "Finish"

### Способ Б: Через пакетный менеджер (для продвинутых)

Если у вас установлен `chocolatey`:
```powershell
choco install postgresql15
```

Или через `winget`:
```powershell
winget install PostgreSQL.PostgreSQL
```

## 🔧 Шаг 2: Проверка установки

1. **Проверьте, что PostgreSQL запущен:**
   - Откройте **Диспетчер задач** (Ctrl + Shift + Esc)
   - Перейдите на вкладку "Службы"
   - Найдите "postgresql-x64-15" (или похожее)
   - Должен быть статус "Выполняется"

2. **Если служба не запущена:**
   - Правой кнопкой → "Запустить"
   - Или через PowerShell (от администратора):
     ```powershell
     Start-Service postgresql-x64-15
     ```

## 🗄️ Шаг 3: Создание базы данных и пользователя

### Вариант А: Через графический интерфейс pgAdmin

1. **Откройте pgAdmin 4:**
   - Найдите в меню Пуск "pgAdmin 4"
   - Или перейдите: `C:\Program Files\PostgreSQL\15\pgAdmin 4\bin\pgAdmin4.exe`

2. **Подключитесь к серверу:**
   - При первом запуске попросит мастер-пароль (можно оставить пустым или установить свой)
   - В левой панели найдите "Servers" → "PostgreSQL 15"
   - При подключении введите пароль суперпользователя (который указали при установке)

3. **Создайте базу данных:**
   - Правой кнопкой на "PostgreSQL 15" → "Create" → "Database..."
   - **Name**: `onepace`
   - **Owner**: `postgres`
   - Нажмите "Save"

4. **Создайте пользователя (опционально):**
   - Правой кнопкой на "Login/Group Roles" → "Create" → "Login/Group Role..."
   - **General** → **Name**: `onepace_user`
   - **Definition** → **Password**: `onepace_password`
   - **Privileges** → Включите "Can login"
   - Нажмите "Save"

5. **Выдайте права:**
   - Правой кнопкой на базе данных `onepace` → "Properties"
   - Вкладка "Security"
   - Добавьте пользователя `onepace_user` с правами "All"

### Вариант Б: Через командную строку (psql)

1. **Откройте psql:**
   - Найдите в меню Пуск "SQL Shell (psql)"
   - Или выполните в командной строке:
     ```powershell
     & "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres
     ```
   - Введите пароль суперпользователя (который указали при установке)

2. **Выполните команды:**
   ```sql
   -- Создать базу данных
   CREATE DATABASE onepace;

   -- Создать пользователя
   CREATE USER onepace_user WITH PASSWORD 'onepace_password';

   -- Выдать права
   GRANT ALL PRIVILEGES ON DATABASE onepace TO onepace_user;

   -- Подключиться к базе данных
   \c onepace

   -- Выдать права на схему public
   GRANT ALL ON SCHEMA public TO onepace_user;
   ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO onepace_user;

   -- Выход
   \q
   ```

## 📝 Шаг 4: Настройка переменных окружения

1. **Создайте файл `.env` в папке `backend`:**
   ```bash
   cd backend
   copy env.example .env
   ```

2. **Откройте `backend\.env` и измените настройки:**
   ```env
   # Server Configuration
   PORT=3002
   NODE_ENV=development

   # Database - ИЗМЕНИТЕ НА ЛОКАЛЬНЫЕ НАСТРОЙКИ
   POSTGRES_HOST=localhost
   POSTGRES_PORT=5432          # ← Стандартный порт PostgreSQL
   POSTGRES_DB=onepace        # ← Имя базы данных
   POSTGRES_USER=onepace_user # ← Имя пользователя (или postgres)
   POSTGRES_PASSWORD=onepace_password # ← Пароль пользователя

   # JWT
   JWT_SECRET=your-super-secret-jwt-key-change-in-production
   JWT_EXPIRES_IN=7d

   # CORS
   CORS_ORIGIN=http://localhost:3000

   # File Upload
   MAX_FILE_SIZE=5242880
   ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif
   ```

   **Вариант упрощенный (если используете суперпользователя postgres):**
   ```env
   POSTGRES_HOST=localhost
   POSTGRES_PORT=5432
   POSTGRES_DB=onepace
   POSTGRES_USER=postgres      # ← Используем суперпользователя
   POSTGRES_PASSWORD=ваш_пароль_суперпользователя  # ← Пароль, который указали при установке
   ```

## 🗃️ Шаг 5: Инициализация базы данных

1. **Откройте psql и подключитесь к базе:**
   ```powershell
   & "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres -d onepace
   ```

2. **Или через pgAdmin:**
   - Откройте Query Tool для базы данных `onepace`

3. **Выполните SQL скрипты из проекта:**

   **Сначала `01-init.sql`:**
   - Скопируйте содержимое файла `backend/init-scripts/01-init.sql`
   - Вставьте в psql или Query Tool
   - Выполните (F5 в pgAdmin)

   **Затем `02-create-tables.sql`:**
   - Скопируйте содержимое файла `backend/init-scripts/02-create-tables.sql`
   - Вставьте в psql или Query Tool
   - Выполните (F5 в pgAdmin)

4. **Проверьте, что таблицы созданы:**
   ```sql
   -- Список таблиц
   \dt

   -- Должны быть таблицы: users, jobs
   ```

## ✅ Шаг 6: Проверка подключения

1. **Запустите backend:**
   ```bash
   cd backend
   npm run dev
   ```

2. **В консоли должно появиться:**
   ```
   🚀 Server running on port 3002
   📊 Environment: development
   🔗 API URL: http://localhost:3002/api
   💾 Database: localhost:5432/onepace
   Database connected successfully: ...
   ```

3. **Если видите ошибку подключения:**
   - Проверьте, что PostgreSQL запущен (Диспетчер задач → Службы)
   - Проверьте настройки в `backend\.env`
   - Проверьте пароль и имя пользователя
   - Проверьте, что база данных `onepace` существует

## 🐛 Решение проблем

### Ошибка: "password authentication failed"
- Проверьте пароль в `.env` файле
- Убедитесь, что используете правильного пользователя

### Ошибка: "database does not exist"
- Создайте базу данных через pgAdmin или psql (см. Шаг 3)

### Ошибка: "connection refused" или "could not connect"
- Проверьте, что служба PostgreSQL запущена
- Проверьте порт в `.env` (должен быть 5432)
- Проверьте firewall (PostgreSQL должен быть разрешен)

### Ошибка: "permission denied"
- Убедитесь, что пользователь имеет права на базу данных
- Попробуйте использовать суперпользователя `postgres`

### Служба PostgreSQL не запускается
- Откройте "Службы" (services.msc)
- Найдите "postgresql-x64-15"
- Правой кнопкой → "Свойства"
- Убедитесь, что "Тип запуска" = "Автоматически"
- Попробуйте запустить вручную

## 📝 Быстрая команда для создания БД (если используете postgres)

Если используете суперпользователя `postgres`, можно выполнить одну команду:

```powershell
# Создать БД и выполнить скрипты одной командой
& "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres -c "CREATE DATABASE onepace;"
& "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres -d onepace -f "backend\init-scripts\01-init.sql"
& "C:\Program Files\PostgreSQL\15\bin\psql.exe" -U postgres -d onepace -f "backend\init-scripts\02-create-tables.sql"
```

## 🎯 Готово!

Теперь PostgreSQL настроен локально и готов к работе с проектом OnePlace.

**Преимущества локальной установки:**
- ✅ Не нужен Docker
- ✅ Работает напрямую с Windows
- ✅ Проще настроить
- ✅ Быстрее для разработки
- ✅ Нет конфликтов с драйверами видеокарты

