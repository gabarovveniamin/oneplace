# 🔧 Исправление ошибки 404

## 🚨 Проблема
При попытке создать вакансию возникает ошибка `HTTP error! status: 404`, что означает, что frontend не может подключиться к backend API.

## 🔍 Диагностика

### 1. Проверьте, запущен ли backend сервер

Откройте новый терминал и выполните:

```bash
# Перейти в папку backend
cd backend

# Запустить сервер (выберите один из вариантов)
npm run dev
# ИЛИ
node simple-server.js
# ИЛИ (если установлен Python)
python simple-server.py
```

### 2. Проверьте, работает ли API

Откройте браузер и перейдите по адресу:
- http://localhost:3002/health

Вы должны увидеть JSON ответ:
```json
{
  "success": true,
  "message": "Mock API Server is running",
  "timestamp": "2024-01-01T12:00:00.000Z",
  "environment": "development"
}
```

### 3. Проверьте конфигурацию API

Убедитесь, что в файле `src/config/env.ts` указан правильный URL:
```typescript
baseUrl: import.meta.env.VITE_API_URL || 'http://localhost:3002/api'
```

## 🛠️ Решения

### Вариант 1: Запуск Node.js сервера
```bash
cd backend
node simple-server.js
```

### Вариант 2: Запуск Python сервера
```bash
python simple-server.py
```

### Вариант 3: Запуск через npm
```bash
cd backend
npm install
npm run dev
```

### Вариант 4: Проверка портов
Возможно, порт 3002 занят. Проверьте:
```bash
netstat -an | findstr :3002
```

Если порт занят, измените PORT в сервере на другой (например, 3003).

## 🧪 Тестирование

### 1. Откройте тестовую страницу
Откройте файл `test-api.html` в браузере для проверки API.

### 2. Проверьте в браузере
1. Откройте http://localhost:3001
2. Перейдите в профиль
3. Нажмите "Разместить вакансию"
4. Заполните форму и попробуйте создать вакансию

### 3. Проверьте консоль браузера
Нажмите F12 и посмотрите на вкладку Network для детальной информации об ошибках.

## 🔧 Альтернативные решения

### Если ничего не работает:

1. **Измените порт API**
   - В `simple-server.js` измените `PORT = 3002` на `PORT = 3003`
   - В `src/config/env.ts` измените URL на `http://localhost:3003/api`

2. **Используйте mock данные**
   - Временно отключите API вызовы в `useJobCreation.ts`
   - Используйте локальные mock данные

3. **Проверьте CORS**
   - Убедитесь, что CORS настроен правильно в сервере
   - Frontend должен быть на http://localhost:3001

## 📝 Логи для отладки

### В консоли браузера должны появиться логи:
```
📝 Создание новой вакансии: [название вакансии]
✅ Вакансия создана: [название вакансии]
```

### В консоли сервера должны появиться логи:
```
🚀 Mock API Server running on port 3002
🔗 API URL: http://localhost:3002/api
💾 Using mock data (1 jobs)
🌐 Health check: http://localhost:3002/health
```

## 🆘 Если проблема не решается

1. Перезапустите терминал
2. Убедитесь, что Node.js установлен: `node --version`
3. Убедитесь, что Python установлен: `python --version`
4. Проверьте, не блокирует ли антивирус или файрвол подключения
5. Попробуйте другой порт (3003, 3004, 8080)

## ✅ Успешное решение

Когда все работает правильно, вы должны увидеть:
- ✅ Сообщение "Вакансия успешно создана!"
- ✅ Перенаправление обратно в профиль
- ✅ Новая вакансия в списке вакансий
