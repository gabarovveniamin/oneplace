# 🚀 Инструкция по запуску системы размещения вакансий

## 📋 Обзор системы

Система размещения вакансий состоит из:
- **Frontend** (React + TypeScript + Vite)
- **Backend** (Node.js + Express + TypeScript)
- **База данных** (PostgreSQL)
- **API** для создания, поиска и управления вакансиями

## 🛠️ Запуск системы

### 1. Запуск Backend сервера

```bash
# Перейти в папку backend
cd backend

# Установить зависимости (если еще не установлены)
npm install

# Запустить PostgreSQL через Docker
docker-compose up -d postgres

# Проверить статус базы данных
docker ps

# Запустить backend сервер
npm run dev
```

Backend будет доступен по адресу: `http://localhost:3002`

### 2. Запуск Frontend приложения

```bash
# В новом терминале, перейти в корневую папку проекта
cd ..

# Запустить frontend
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3001`

## 📊 API Endpoints

### Вакансии
- `GET /api/jobs` - Получить все вакансии
- `POST /api/jobs` - Создать новую вакансию
- `GET /api/jobs/:id` - Получить вакансию по ID
- `PUT /api/jobs/:id` - Обновить вакансию
- `DELETE /api/jobs/:id` - Удалить вакансию
- `GET /api/jobs/search` - Поиск вакансий с фильтрами

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход

## 🎯 Функциональность

### ✅ Реализовано

1. **Форма размещения вакансий**
   - 3-шаговая форма с валидацией
   - Основная информация (название, компания, зарплата, местоположение)
   - Описание и требования
   - Контактная информация
   - Предварительный просмотр

2. **API интеграция**
   - Хук `useJobCreation` для создания вакансий
   - Обработка ошибок и состояний загрузки
   - TypeScript типы для всех API запросов

3. **Дополнительные поля**
   - Специализация и отрасль
   - Диапазон зарплаты (от/до)
   - Требуемый опыт и образование
   - Формат работы (офис/удаленно/гибрид)
   - Рабочие часы в день

4. **Backend готов**
   - PostgreSQL база данных с оптимизированными индексами
   - Полнотекстовый поиск на русском языке
   - Валидация данных
   - Аутентификация и авторизация
   - Docker-compose для быстрого запуска

### 🔄 Следующие шаги

1. **Запустить Docker Desktop**
2. **Создать .env файл** в папке backend (скопировать из env.example)
3. **Протестировать создание вакансии**
4. **Добавить аутентификацию** в frontend
5. **Реализовать загрузку файлов** (логотипы компаний)

## 🧪 Тестирование

### Проверка Backend
```bash
# Проверить здоровье сервера
curl http://localhost:3002/health

# Проверить базу данных
docker exec -it onepace-postgres psql -U onepace_user -d onepace
```

### Проверка Frontend
1. Открыть `http://localhost:3001`
2. Перейти в профиль
3. Нажать "Разместить вакансию"
4. Заполнить форму и создать вакансию

## 📝 Структура базы данных

### Таблица `jobs`
- Основные поля: title, company, salary, location, type, description
- Расширенные поля: specialization, industry, region, salary_from, salary_to
- Метаданные: posted_by, is_active, is_featured, views, applications
- Временные поля: created_at, updated_at, expires_at

### Таблица `users`
- Основные поля: email, password, first_name, last_name
- Роли: user, employer, admin
- Статусы: is_active, is_email_verified

## 🔧 Настройка

### Переменные окружения (.env)
```env
PORT=3002
NODE_ENV=development
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=onepace
POSTGRES_USER=onepace_user
POSTGRES_PASSWORD=onepace_password
JWT_SECRET=your-super-secret-jwt-key
CORS_ORIGIN=http://localhost:3001
```

### CORS настройки
Frontend: `http://localhost:3001`
Backend: `http://localhost:3002`

## 🚨 Решение проблем

### Docker не запускается
```bash
# Запустить Docker Desktop
# Или использовать локальную PostgreSQL
```

### Ошибки подключения к БД
```bash
# Проверить статус контейнера
docker ps

# Перезапустить PostgreSQL
docker-compose restart postgres
```

### CORS ошибки
- Убедиться, что backend запущен на порту 3002
- Проверить CORS_ORIGIN в .env файле

## 📈 Производительность

- **Индексы БД**: Оптимизированы для поиска по всем основным полям
- **Полнотекстовый поиск**: Поддержка русского языка
- **Пагинация**: Все списки поддерживают пагинацию
- **Кэширование**: Redis для кэширования популярных запросов

## 🔒 Безопасность

- **Helmet**: Защита HTTP заголовков
- **Rate Limiting**: Ограничение количества запросов
- **JWT**: Безопасная аутентификация
- **Валидация**: Проверка всех входящих данных
- **SQL Injection**: Защита через параметризованные запросы
