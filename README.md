# 🚀 OnePlace - Платформа для поиска работы

Современная веб-платформа для поиска работы, подработок и интересных проектов.

## 📋 Описание

OnePlace - это полнофункциональная платформа для размещения и поиска вакансий с современным интерфейсом и мощными возможностями фильтрации.

## ✨ Основные возможности

### 🎯 Для соискателей:
- **Поиск вакансий** с расширенными фильтрами
- **Детальная информация** о каждой вакансии
- **Адаптивный дизайн** для всех устройств
- **Темная/светлая тема**

### 🏢 Для работодателей:
- **Размещение вакансий** через удобную форму
- **Управление профилем** компании
- **Статистика просмотров** и откликов

### 🔍 Поиск и фильтрация:
- **Полнотекстовый поиск** по названию, описанию, компании
- **Фильтры по специализации**, опыту, образованию
- **Фильтр по зарплате** и формату работы
- **Фильтр по региону** и графику работы

## 🛠️ Технологический стек

### Frontend:
- **React 18** с TypeScript
- **Vite** для быстрой разработки
- **Tailwind CSS** для стилизации
- **Lucide React** для иконок
- **React Hooks** для управления состоянием

### Backend:
- **Node.js** с Express.js
- **TypeScript** для типобезопасности
- **PostgreSQL** как основная база данных
- **JWT** для аутентификации
- **Docker** для контейнеризации

### Инструменты разработки:
- **ESLint** и **Prettier** для качества кода
- **Husky** для git hooks
- **TypeScript** для статической типизации

## 🚀 Быстрый старт

### Предварительные требования:
- Node.js 18+ 
- PostgreSQL 15+
- Docker (опционально)

### Установка и запуск:

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/gabarovveniamin/oneplace.git
cd oneplace
```

2. **Установите зависимости:**
```bash
# Frontend
npm install

# Backend
cd backend
npm install
cd ..
```

3. **Настройте базу данных:**
```bash
# Запустите PostgreSQL через Docker
cd backend
docker-compose up -d postgres
```

4. **Настройте переменные окружения:**
```bash
# Скопируйте пример конфигурации
cp backend/env.example backend/.env
# Отредактируйте backend/.env под ваши настройки
```

5. **Запустите приложение:**
```bash
# Backend (в отдельном терминале)
cd backend
npm run dev

# Frontend (в отдельном терминале)
npm run dev
```

6. **Откройте в браузере:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:3002/api

## 📁 Структура проекта

```
oneplace/
├── src/                    # Frontend код
│   ├── features/          # Функциональные модули
│   │   ├── auth/         # Аутентификация
│   │   ├── jobs/         # Работа с вакансиями
│   │   └── profile/      # Профиль пользователя
│   ├── shared/           # Общие компоненты
│   │   └── ui/          # UI компоненты
│   └── core/            # Основная логика
│       └── api/         # API клиенты
├── backend/              # Backend код
│   ├── src/
│   │   ├── controllers/  # Контроллеры
│   │   ├── models/      # Модели данных
│   │   ├── routes/      # Маршруты API
│   │   └── middleware/  # Middleware
│   ├── init-scripts/    # SQL скрипты
│   └── docker-compose.yml
└── docs/                # Документация
```

## 🔧 API Endpoints

### Вакансии:
- `GET /api/jobs` - Получить все вакансии
- `POST /api/jobs` - Создать новую вакансию
- `GET /api/jobs/:id` - Получить вакансию по ID
- `PUT /api/jobs/:id` - Обновить вакансию
- `DELETE /api/jobs/:id` - Удалить вакансию
- `GET /api/jobs/search` - Поиск вакансий с фильтрами

### Аутентификация:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход

## 🧪 Тестирование

Для проверки работы API:
1. Откройте http://localhost:3000
2. Убедитесь, что вакансии загружаются на главной странице
3. Проверьте консоль браузера (F12) на наличие ошибок

## 📊 База данных

### Основные таблицы:
- `users` - Пользователи системы
- `jobs` - Вакансии
- `applications` - Отклики на вакансии

### Индексы для оптимизации:
- Полнотекстовый поиск на русском языке
- Индексы по всем основным полям фильтрации
- Составные индексы для сложных запросов

## 🚀 Развертывание

### Docker:
```bash
# Сборка и запуск всех сервисов
docker-compose up --build
```

### Production:
1. Настройте переменные окружения
2. Соберите frontend: `npm run build`
3. Соберите backend: `cd backend && npm run build`
4. Запустите: `cd backend && npm start`

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👨‍💻 Автор

**Veniamin Gabarov** - [@gabarovveniamin](https://github.com/gabarovveniamin)

- GitHub: [@gabarovveniamin](https://github.com/gabarovveniamin)
- Email: vena.gabarov@gmail.com

## 📞 Поддержка

Если у вас есть вопросы или предложения:
- Создайте [Issue](https://github.com/gabarovveniamin/oneplace/issues)
- Напишите на email: vena.gabarov@gmail.com

---

⭐ **Если проект вам понравился, поставьте звезду!**