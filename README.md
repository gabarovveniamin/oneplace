# 🚀 OnePlace - Платформа для поиска работы

Современная веб-платформа для поиска работы, подработок и интересных проектов. Проект объединяет соискателей и работодателей в единую экосистему с удобным управлением вакансиями.

---

## 📋 Описание проекта

OnePlace — это полнофункциональное SPA-приложение (Single Page Application) с мощным поисковым движком и личным кабинетом. Платформа поддерживает автоматизацию создания вакансий для компаний и удобный трекинг откликов для пользователей.

## ✨ Ключевые функции

### 🎯 Для соискателей
- **Персонализированный поиск**: Фильтрация по региону, опыту, образованию и зарплате.
- **Управление профилем**: Заполнение навыков, опыта и возможность скачивания резюме.
- **Избранное**: Сохранение интересных предложений «на потом».

### 🏢 Для работодателей
- **Профиль организации**: Регистрация своей компании (название, отрасль, сайт, описание).
- **Умная форма вакансий**: Автоматическое заполнение данных компании при создании новых объявлений.
- **Мониторинг откликов**: Прямая связь с кандидатами через указанные контакты.

### 🌓 Интерфейс
- **Dark Mode**: Поддержка темной и светлой темы.
- **Адаптивность**: Идеальное отображение на мобильных устройствах и десктопах.

---

## 🛠️ Технологический стек

- **Frontend**: React 18, TypeScript, Vite, Tailwind CSS, Lucide Icons.
- **Backend**: Node.js, Express, TypeScript.
- **Database**: PostgreSQL 15+.
- **Auth**: JWT (JSON Web Tokens).

---

## 🚀 Быстрый старт

### 1. Подготовка (Checklist)
Убедитесь, что у вас установлены:
- [Node.js](https://nodejs.org/) (версия 18 или выше).
- [PostgreSQL](https://www.postgresql.org/) (версия 15 или выше).

### 2. Установка зависимостей
```powershell
# В корне проекта (для фронтенда)
npm install

# В папке backend (для бэкенда)
cd backend
npm install
cd ..
```

### 3. Настройка Базы Данных
1. Откройте **pgAdmin** или **psql**.
2. Создайте пустую базу данных с именем `onepace`.
3. Заполните таблицы, выполнив скрипт:
   ```powershell
   psql -U postgres -d onepace -f backend/init-database.sql
   ```

### 4. Конфигурация (.env)
Создайте файл `backend/.env` (можете скопировать из `env.example`):
```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=onepace
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ВАШ_ПАРОЛЬ
JWT_SECRET=super_secret_key_123
```

### 5. Запуск
Откройте два терминала:

**Терминал 1 (Бэкенд):**
```bash
cd backend
npm run dev
```

**Терминал 2 (Фронтенд):**
```bash
npm run dev
```
Фронтенд будет доступен по адресу: `http://localhost:3000`

---

## 📁 Структура папок
```
oneplace/
├── src/                    # Frontend (React)
│   ├── features/          # Модули (auth, jobs, profile, admin)
│   ├── shared/           # UI-кит, переиспользуемые компоненты, типы
│   └── core/            # API-клиент и конфигурация
├── backend/              # Backend (Express)
│   ├── src/             # Контроллеры, модели (User, Job), роуты
│   └── init-database.sql # Схема БД для PostgreSQL
└── public/               # Статические ресурсы (логотипы, иконки)
```

---

## 👨‍💻 Разработчик
- **Veniamin Gabarov** — [@gabarovveniamin](https://github.com/gabarovveniamin)

⭐ **Понравился проект? Поставь звезду на GitHub!**