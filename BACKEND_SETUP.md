# Настройка бэкенда для OnePlace

## 🚀 API Endpoints

Приложение готово к работе с REST API. Все захардкоженные данные удалены и заменены на API вызовы.

### 📋 Требуемые endpoints:

#### Вакансии (Jobs)
```
GET    /api/jobs                    - Получить все вакансии с пагинацией
GET    /api/jobs/:id                - Получить вакансию по ID
GET    /api/jobs/search             - Поиск вакансий с фильтрами
POST   /api/jobs                    - Создать новую вакансию
PUT    /api/jobs/:id                - Обновить вакансию
DELETE /api/jobs/:id                - Удалить вакансию
GET    /api/jobs/popular            - Получить популярные вакансии
GET    /api/jobs/recent             - Получить недавние вакансии
GET    /api/jobs/stats              - Получить статистику вакансий
GET    /api/users/me/jobs           - Получить вакансии текущего пользователя
```

### 📊 Форматы данных

#### Создание/Обновление вакансии
```typescript
interface CreateJobRequest {
  title: string;
  company: string;
  salary: string;
  location: string;
  type: string;
  description: string;
  tags: string[];
  logo?: string;
  specialization?: string;
  industry?: string;
  region?: string;
  salaryFrom?: number;
  salaryTo?: number;
  salaryFrequency?: 'hourly' | 'daily' | 'weekly' | 'monthly' | 'yearly';
  education?: 'no-education' | 'secondary' | 'vocational' | 'bachelor' | 'master' | 'phd';
  experience?: 'no-experience' | '1-year' | '1-3-years' | '3-5-years' | '5-10-years' | '10-plus-years';
  employmentType?: 'full-time' | 'part-time' | 'contract' | 'freelance' | 'internship';
  schedule?: 'flexible' | 'fixed' | 'shift' | 'night' | 'weekend';
  workHours?: number;
  workFormat?: 'office' | 'remote' | 'hybrid';
}
```

#### Ответ API
```typescript
interface ApiResponse<T> {
  data: T;
  message?: string;
  success: boolean;
}

interface PaginatedResponse<T> {
  data: T[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
  };
}
```

### 🔧 Настройка

1. **Создайте файл `.env` в корне проекта:**
```env
VITE_API_URL=http://localhost:3000/api
VITE_APP_NAME=OnePlace
VITE_APP_VERSION=1.0.0
VITE_DEV_MODE=true
```

2. **Запустите бэкенд сервер на порту 3000**

3. **Приложение автоматически подключится к API**

### 🎯 Функциональность

- ✅ **CRUD операции** для вакансий
- ✅ **Поиск и фильтрация** с пагинацией
- ✅ **Обработка ошибок** и состояний загрузки
- ✅ **Авторизация** через Bearer токены
- ✅ **Валидация** данных
- ✅ **Типизация** TypeScript

### 🔄 Состояния приложения

- **Загрузка** - показываются скелетоны
- **Ошибки** - отображаются алерты с описанием
- **Пустой результат** - показывается сообщение "не найдено"
- **Успешная загрузка** - отображаются вакансии

### 📱 Готовые компоненты

- `useJobs` - хук для работы со списком вакансий
- `useJob` - хук для работы с отдельной вакансией
- `JobsList` - компонент списка с состояниями загрузки
- `AdvancedSearch` - расширенный поиск
- `JobCard` - карточка вакансии
- `JobDetails` - детальная страница вакансии

### 🚀 Следующие шаги

1. Реализуйте бэкенд API
2. Добавьте авторизацию
3. Настройте базу данных
4. Добавьте загрузку файлов (логотипы)
5. Реализуйте уведомления
