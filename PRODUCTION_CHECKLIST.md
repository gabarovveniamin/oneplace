# 🚀 Чек-лист для Production-версии

## 📋 Содержание
- [1. Безопасность и Аутентификация](#1-безопасность-и-аутентификация)
- [2. База данных](#2-база-данных)
- [3. Frontend](#3-frontend)
- [4. Backend API](#4-backend-api)
- [5. Конфигурация окружения](#5-конфигурация-окружения)
- [6. Деплой и инфраструктура](#6-деплой-и-инфраструктура)
- [7. Мониторинг и логирование](#7-мониторинг-и-логирование)
- [8. Тестирование](#8-тестирование)
- [9. Документация](#9-документация)

---

## 1. Безопасность и Аутентификация

### ✅ Обязательные задачи:

#### 1.1 Восстановить аутентификацию
- [ ] **Раскомментировать middleware** в `backend/src/routes/jobs.ts`:
  ```typescript
  router.post('/', 
    authenticate,  // ← Вернуть
    authorize('user', 'employer', 'admin'),  // ← Вернуть
    createJobValidation, 
    validate(createJobValidation), 
    createJob
  );
  ```

- [ ] **Удалить временный код** из `backend/src/controllers/jobController.ts`:
  - Убрать логику получения первого работодателя из БД
  - Вернуть обязательное использование `user.id`

#### 1.2 Создать страницу входа/регистрации

**Frontend компоненты, которые нужно создать:**

- [ ] `src/features/auth/components/Login.tsx` - форма входа
- [ ] `src/features/auth/components/LoginDialog.tsx` - модальное окно входа
- [ ] **Функционал:**
  - Email + пароль
  - Валидация полей
  - Обработка ошибок
  - Сохранение токена в localStorage
  - Перенаправление после успешного входа

**Пример структуры Login.tsx:**
```typescript
import { useState } from 'react';
import { authApiService } from '../../../core/api';

export function Login({ onSuccess }: { onSuccess?: () => void }) {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    setError('');
    
    try {
      await authApiService.login({ email, password });
      onSuccess?.();
    } catch (err: any) {
      setError(err.message || 'Ошибка входа');
    } finally {
      setLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* Форма входа */}
    </form>
  );
}
```

#### 1.3 Защита маршрутов

- [ ] **Проверка авторизации** перед доступом к защищённым страницам:
  - Профиль пользователя
  - Размещение вакансий
  - Редактирование вакансий
  
- [ ] **Редирект на страницу входа** если пользователь не авторизован

**Пример защищённого компонента:**
```typescript
function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const isAuth = authApiService.isAuthenticated();
  
  if (!isAuth) {
    // Показать модалку входа или редирект
    return <LoginDialog />;
  }
  
  return <>{children}</>;
}
```

#### 1.4 Безопасность паролей

- [ ] **JWT Secret** - сменить на сильный случайный ключ:
  ```bash
  # Генерация безопасного секрета
  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
  ```
  
- [ ] **Хеширование паролей** - проверить что используется bcrypt (уже реализовано ✅)

#### 1.5 CORS настройки

- [ ] Ограничить `CORS_ORIGIN` только доверенными доменами:
  ```typescript
  // backend/src/config/config.ts
  cors: {
    origin: process.env.CORS_ORIGIN || 'https://ваш-домен.com',
    credentials: true,
  }
  ```

#### 1.6 Rate Limiting

- [ ] Настроить лимиты для критичных эндпоинтов:
  ```typescript
  // Более строгие лимиты для аутентификации
  const authLimiter = rateLimit({
    windowMs: 15 * 60 * 1000,
    max: 5, // 5 попыток в 15 минут
  });
  
  app.use('/api/auth/login', authLimiter);
  app.use('/api/auth/register', authLimiter);
  ```

---

## 2. База данных

### 2.1 Выбор БД для Production

**Текущая БД:** SQLite (отлично для разработки, но имеет ограничения для production)

#### Вариант A: Оставить SQLite (для небольших проектов)
**✅ Плюсы:**
- Простота деплоя
- Нет дополнительных зависимостей
- Быстрая работа для малых/средних нагрузок

**❌ Минусы:**
- Ограниченная поддержка конкурентных записей
- Нет репликации
- Сложно масштабировать

**Задачи если оставляете SQLite:**
- [ ] Настроить регулярные бэкапы базы данных
- [ ] Оптимизировать индексы (уже сделано ✅)
- [ ] Настроить автоматическую vacuum процедуру
- [ ] Добавить мониторинг размера файла БД

#### Вариант B: Миграция на PostgreSQL (рекомендуется)
**✅ Плюсы:**
- Высокая производительность
- Поддержка репликации
- Лучшая поддержка конкурентности
- Полнотекстовый поиск (уже подготовлено в init-scripts)

**Задачи:**
- [ ] Настроить PostgreSQL сервер (локально или облако)
- [ ] Изменить `config.ts` - использовать PostgreSQL вместо SQLite
- [ ] Запустить миграции из `backend/init-scripts/`
- [ ] Протестировать работу всех API

**У вас уже есть готовые SQL скрипты для PostgreSQL:**
- `backend/init-scripts/01-init.sql`
- `backend/init-scripts/02-create-tables.sql`

### 2.2 Бэкапы

- [ ] Настроить автоматические бэкапы БД (ежедневно)
- [ ] Настроить хранение бэкапов в облаке (AWS S3, Google Cloud Storage)
- [ ] Протестировать восстановление из бэкапа

### 2.3 Миграции

- [ ] Внедрить систему миграций (например, node-pg-migrate или TypeORM)
- [ ] Версионировать изменения схемы БД

---

## 3. Frontend

### 3.1 Билд для продакшена

- [ ] **Собрать production билд:**
  ```bash
  npm run build
  ```

- [ ] **Оптимизация:**
  - [ ] Минификация JavaScript/CSS
  - [ ] Tree-shaking неиспользуемого кода
  - [ ] Code splitting для уменьшения начальной загрузки
  - [ ] Compression (Gzip/Brotli)

### 3.2 Переменные окружения

- [ ] Создать `.env.production`:
  ```env
  VITE_API_BASE_URL=https://api.ваш-домен.com/api
  ```

- [ ] Убедиться что API URL берётся из конфига:
  ```typescript
  // src/config/env.ts
  export const config = {
    api: {
      baseUrl: import.meta.env.VITE_API_BASE_URL || 'http://localhost:3002/api'
    }
  };
  ```

### 3.3 SEO и метатеги

- [ ] Добавить метатеги в `index.html`:
  ```html
  <meta name="description" content="Работа+ - найдите работу мечты">
  <meta name="keywords" content="работа, вакансии, поиск работы">
  <meta property="og:title" content="Работа+">
  <meta property="og:description" content="...">
  <meta property="og:image" content="...">
  ```

### 3.4 Аналитика

- [ ] Подключить Google Analytics или Яндекс.Метрику
- [ ] Настроить отслеживание ключевых событий:
  - Регистрация пользователей
  - Размещение вакансий
  - Отклики на вакансии

### 3.5 Error Boundary

- [ ] Добавить глобальный Error Boundary для отлова ошибок React
- [ ] Настроить красивую страницу ошибки

---

## 4. Backend API

### 4.1 Переменные окружения

- [ ] **Создать `.env` файл** (не коммитить в Git!):
  ```env
  NODE_ENV=production
  PORT=3002
  
  # JWT
  JWT_SECRET=ваш_супер_секретный_ключ_64_символа_минимум
  JWT_EXPIRES_IN=7d
  
  # CORS
  CORS_ORIGIN=https://ваш-домен.com
  
  # Database (если PostgreSQL)
  POSTGRES_HOST=localhost
  POSTGRES_PORT=5432
  POSTGRES_DB=oneplace_prod
  POSTGRES_USER=oneplace_prod_user
  POSTGRES_PASSWORD=безопасный_пароль
  ```

- [ ] **Добавить валидацию ENV переменных** при старте:
  ```typescript
  if (!process.env.JWT_SECRET || process.env.JWT_SECRET.length < 32) {
    throw new Error('JWT_SECRET must be at least 32 characters');
  }
  ```

### 4.2 Логирование

- [ ] Подключить профессиональную библиотеку логирования (winston, pino):
  ```bash
  npm install winston
  ```

- [ ] Настроить уровни логов:
  - `error` - ошибки
  - `warn` - предупреждения
  - `info` - важные события
  - `debug` - только для development

- [ ] Логировать в файлы + консоль (в production только файлы)

### 4.3 Обработка ошибок

- [ ] Улучшить middleware обработки ошибок
- [ ] Не отправлять stack trace клиенту в production
- [ ] Логировать все ошибки с контекстом

### 4.4 Validation

- [ ] Проверить что все входящие данные валидируются (express-validator уже используется ✅)
- [ ] Добавить санитизацию данных (защита от XSS)

### 4.5 Сжатие ответов

- [ ] Добавить compression middleware:
  ```bash
  npm install compression
  ```
  ```typescript
  import compression from 'compression';
  app.use(compression());
  ```

### 4.6 HTTPS только

- [ ] В production принимать только HTTPS запросы
- [ ] Настроить редирект с HTTP на HTTPS

---

## 5. Конфигурация окружения

### 5.1 TypeScript билд

- [ ] **Собрать backend:**
  ```bash
  cd backend
  npm run build
  ```

- [ ] **Проверить что билд работает:**
  ```bash
  node dist/index.js
  ```

### 5.2 Process Manager

- [ ] **Использовать PM2** для управления процессом:
  ```bash
  npm install -g pm2
  pm2 start npm --name "oneplace-backend" -- start
  pm2 save
  pm2 startup
  ```

- [ ] **Создать ecosystem.config.js:**
  ```javascript
  module.exports = {
    apps: [{
      name: 'oneplace-backend',
      script: './dist/index.js',
      instances: 2,
      exec_mode: 'cluster',
      env_production: {
        NODE_ENV: 'production'
      }
    }]
  };
  ```

---

## 6. Деплой и инфраструктура

### 6.1 Выбор хостинга

**Backend опции:**
- [ ] VPS (DigitalOcean, Hetzner, Selectel)
- [ ] PaaS (Heroku, Railway, Render)
- [ ] AWS (EC2, Elastic Beanstalk)
- [ ] Vercel/Netlify (для небольших API)

**Frontend опции:**
- [ ] Vercel (рекомендуется для Vite)
- [ ] Netlify
- [ ] Cloudflare Pages
- [ ] AWS S3 + CloudFront

### 6.2 CI/CD

- [ ] Настроить GitHub Actions для автодеплоя:
  ```yaml
  # .github/workflows/deploy.yml
  name: Deploy
  on:
    push:
      branches: [main]
  jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - name: Deploy to production
          run: |
            # ваши команды деплоя
  ```

### 6.3 Nginx конфигурация

Если используете VPS:

- [ ] **Установить Nginx** как reverse proxy:
  ```nginx
  server {
    listen 80;
    server_name api.ваш-домен.com;
    
    location / {
      proxy_pass http://localhost:3002;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_set_header Host $host;
      proxy_cache_bypass $http_upgrade;
    }
  }
  ```

### 6.4 SSL сертификат

- [ ] **Получить бесплатный SSL** от Let's Encrypt:
  ```bash
  sudo certbot --nginx -d ваш-домен.com -d api.ваш-домен.com
  ```

### 6.5 Домен

- [ ] Купить домен
- [ ] Настроить DNS записи:
  - `A` record: `ваш-домен.com` → IP сервера frontend
  - `A` record: `api.ваш-домен.com` → IP сервера backend
  - `CNAME` record: `www.ваш-домен.com` → `ваш-домен.com`

---

## 7. Мониторинг и логирование

### 7.1 Мониторинг uptime

- [ ] UptimeRobot или Pingdom для проверки доступности
- [ ] Настроить алерты при падении сервера

### 7.2 Логирование ошибок

- [ ] Подключить Sentry для отслеживания ошибок:
  ```bash
  npm install @sentry/node @sentry/react
  ```

### 7.3 Метрики производительности

- [ ] New Relic или DataDog для мониторинга перформанса
- [ ] Отслеживать:
  - Response time
  - Количество запросов
  - Использование CPU/RAM
  - Использование БД

---

## 8. Тестирование

### 8.1 Backend тесты

- [ ] Unit тесты для моделей
- [ ] Integration тесты для API endpoints
- [ ] E2E тесты критичных флоу

**Настроить Jest:**
```bash
npm install --save-dev jest @types/jest ts-jest
```

### 8.2 Frontend тесты

- [ ] Unit тесты компонентов (Vitest/Jest)
- [ ] E2E тесты (Playwright/Cypress)

### 8.3 Нагрузочное тестирование

- [ ] Протестировать API под нагрузкой (Apache Bench, k6)
- [ ] Проверить количество одновременных пользователей

---

## 9. Документация

### 9.1 API документация

- [ ] Swagger/OpenAPI документация для API
- [ ] Примеры запросов/ответов

### 9.2 Документация для команды

- [ ] README с инструкциями по запуску
- [ ] Инструкции по деплою
- [ ] Troubleshooting guide

---

## 🎯 Приоритетная последовательность действий

### Фаза 1: Критичные (без этого нельзя в production)
1. ✅ Восстановить аутентификацию
2. ✅ Создать страницу входа/регистрации
3. ✅ Сменить JWT_SECRET на безопасный
4. ✅ Настроить CORS для вашего домена
5. ✅ Собрать production билды (frontend + backend)

### Фаза 2: Важные (для стабильной работы)
6. ✅ Настроить базу данных (backups)
7. ✅ Настроить HTTPS
8. ✅ Настроить логирование
9. ✅ Деплой на сервер
10. ✅ Настроить мониторинг

### Фаза 3: Желательные (для улучшения)
11. ✅ Написать тесты
12. ✅ Настроить CI/CD
13. ✅ Добавить аналитику
14. ✅ Оптимизация производительности

---

## 📞 Нужна помощь?

Если нужна помощь с конкретными пунктами:
1. **Создание страницы входа** - могу сгенерировать компонент
2. **Настройка деплоя** - подробные инструкции
3. **Миграция на PostgreSQL** - пошаговый гайд
4. **CI/CD pipeline** - готовые конфиги

Сообщите какие разделы важнее всего и я помогу их реализовать!
