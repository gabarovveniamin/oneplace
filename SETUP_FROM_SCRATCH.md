# 🚀 Полная настройка OnePlace с нуля

Пошаговая инструкция для настройки проекта после переустановки Windows или клонирования с GitHub.

## 📋 Требования

Перед началом убедитесь, что установлены:
- **Node.js** 18 или выше ([скачать](https://nodejs.org/))
- **Docker Desktop** ([скачать](https://www.docker.com/products/docker-desktop/)) для запуска PostgreSQL
- **Git** ([скачать](https://git-scm.com/))

## 🔧 Шаг 1: Клонирование проекта

Если проект еще не склонирован:

```bash
# Перейдите в нужную директорию
cd C:\Users\venag\Downloads

# Клонируйте репозиторий (замените URL на ваш)
git clone <ваш-git-url> oneplace
cd oneplace
```

Если проект уже есть, просто перейдите в директорию:
```bash
cd C:\Users\venag\Downloads\oneplace
```

## 🔧 Шаг 2: Установка зависимостей

### Frontend:
```bash
# В корне проекта oneplace/
npm install
```

### Backend:
```bash
# Перейдите в папку backend
cd backend
npm install
cd ..
```

## 🔧 Шаг 3: Настройка базы данных PostgreSQL

### Вариант А: Использование Docker (Рекомендуется)

1. **Запустите Docker Desktop** (убедитесь, что он работает)

2. **Запустите PostgreSQL через Docker Compose:**
   ```bash
   cd backend
   docker-compose up -d postgres
   cd ..
   ```

3. **Проверьте, что контейнер запущен:**
   ```bash
   docker ps
   ```
   Должен быть виден контейнер `onepace-postgres`

### Вариант Б: Локальная установка PostgreSQL

Если Docker не установлен, установите PostgreSQL локально:

1. Скачайте PostgreSQL 15+ с [официального сайта](https://www.postgresql.org/download/windows/)
2. Установите его
3. Создайте базу данных:
   ```sql
   CREATE DATABASE onepace;
   CREATE USER onepace_user WITH PASSWORD 'onepace_password';
   GRANT ALL PRIVILEGES ON DATABASE onepace TO onepace_user;
   ```
4. Запустите SQL скрипты из `backend/init-scripts/`:
   - Сначала `01-init.sql`
   - Потом `02-create-tables.sql`

## 🔧 Шаг 4: Настройка переменных окружения

### Backend:
```bash
cd backend
copy env.example .env
```

Затем откройте `backend/.env` и проверьте настройки (по умолчанию они должны подходить для Docker):

```env
# Server Configuration
PORT=3002
NODE_ENV=development

# Database (для Docker)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=onepace
POSTGRES_USER=onepace_user
POSTGRES_PASSWORD=onepace_password

# JWT (обязательно измените в продакшене!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (порт frontend)
CORS_ORIGIN=http://localhost:3000

# File Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif
```

**Важно:** 
- Если используете локальную PostgreSQL, измените `POSTGRES_PORT` на `5432` (стандартный порт)
- При необходимости измените `JWT_SECRET` на более безопасный ключ

## 🔧 Шаг 5: Проверка подключения к БД

После запуска PostgreSQL (через Docker или локально), проверьте подключение:

```bash
cd backend
npm run dev
```

В консоли должно появиться:
```
🚀 Server running on port 3002
📊 Environment: development
🔗 API URL: http://localhost:3002/api
💾 Database: localhost:5433/onepace
Database connected successfully: ...
```

Если видите ошибку подключения к БД:
1. Убедитесь, что PostgreSQL запущен
2. Проверьте параметры в `.env` файле
3. Проверьте, что порт не занят: `netstat -ano | findstr :5433`

## 🔧 Шаг 6: Запуск проекта

Откройте **два терминала**:

### Терминал 1 - Backend:
```bash
cd backend
npm run dev
```

### Терминал 2 - Frontend:
```bash
# В корне проекта oneplace/
npm run dev
```

## ✅ Готово!

После запуска:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3002/api
- **Health Check**: http://localhost:3002/health

## 🧪 Проверка работы

1. Откройте http://localhost:3000
2. Убедитесь, что главная страница загружается
3. Проверьте консоль браузера (F12) на наличие ошибок подключения к API
4. Попробуйте выполнить поиск вакансий

## 🐛 Решение проблем

### Ошибка: "Cannot connect to database"
- Проверьте, что PostgreSQL запущен: `docker ps` (для Docker) или служба PostgreSQL (для локальной установки)
- Проверьте настройки в `backend/.env`
- Проверьте, что порт не занят другим приложением

### Ошибка: "Port already in use"
- Backend порт 3002 занят: измените `PORT` в `backend/.env`
- Frontend порт 3000 занят: остановите другое приложение или измените порт в `vite.config.ts`

### Ошибка: "Module not found"
- Переустановите зависимости: `npm install` в папке проекта и в `backend/`

### Docker не запускается
- Убедитесь, что Docker Desktop установлен и запущен
- Проверьте, что порты 5433 и 6379 не заняты: `netstat -ano | findstr :5433`

## 📝 Примечания

- При первом запуске Docker автоматически создаст БД и применит SQL скрипты из `backend/init-scripts/`
- Данные PostgreSQL сохраняются в Docker volume, поэтому при перезапуске контейнера данные не потеряются
- Для остановки PostgreSQL: `cd backend && docker-compose down` (или `docker-compose stop postgres`)

## 🔄 Остановка проекта

```bash
# Остановить backend/frontend: Ctrl+C в терминалах

# Остановить PostgreSQL (Docker):
cd backend
docker-compose stop postgres

# Или остановить все сервисы:
docker-compose down
```

