# OnePlace Backend API

Backend API для платформы поиска работы OnePlace, построенный на Node.js, Express.js и PostgreSQL.

## 🚀 Возможности

- ✅ **Аутентификация и авторизация** (JWT)
- ✅ **CRUD операции для вакансий**
- ✅ **Расширенный поиск с фильтрами**
- ✅ **Пагинация**
- ✅ **Валидация данных**
- ✅ **Обработка ошибок**
- ✅ **Rate limiting**
- ✅ **Безопасность (Helmet, CORS)**
- ✅ **TypeScript**

## 📋 Требования

- Node.js 18+
- PostgreSQL 15+
- npm или yarn

## 🛠️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd backend
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp env.example .env
```

Отредактируйте `.env` файл:
```env
PORT=3000
NODE_ENV=development
DATABASE_URL=postgresql://user:password@localhost:5432/oneplace
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3001
```

# Убедитесь, что PostgreSQL запущен и создана база данных

5. **Запустите сервер:**
```bash
# Режим разработки
npm run dev

# Продакшн
npm run build
npm start
```

## 📚 API Endpoints

### Аутентификация
```
POST   /api/auth/register          - Регистрация
POST   /api/auth/login             - Вход
GET    /api/auth/profile           - Получить профиль
PUT    /api/auth/profile           - Обновить профиль
PUT    /api/auth/change-password   - Сменить пароль
```

### Вакансии
```
GET    /api/jobs                   - Получить все вакансии
GET    /api/jobs/search            - Поиск вакансий
GET    /api/jobs/:id               - Получить вакансию по ID
POST   /api/jobs                   - Создать вакансию
PUT    /api/jobs/:id               - Обновить вакансию
DELETE /api/jobs/:id               - Удалить вакансию
GET    /api/jobs/popular           - Популярные вакансии
GET    /api/jobs/recent            - Недавние вакансии
GET    /api/jobs/stats             - Статистика вакансий
GET    /api/jobs/users/me/jobs     - Мои вакансии
```

## 🔧 Структура проекта

```
src/
├── config/           # Конфигурация
├── controllers/      # Контроллеры
├── middleware/       # Middleware
├── models/          # Модели PostgreSQL
├── routes/          # Маршруты
├── utils/           # Утилиты
└── index.ts         # Точка входа
```

## 📊 Модели данных

### User
- email, password, firstName, lastName
- phone, avatar, role
- isEmailVerified, isActive
- lastLogin, timestamps

### Job
- title, company, salary, location
- type, description, tags, logo
- specialization, industry, region
- salaryFrom, salaryTo, salaryFrequency
- education, experience, employmentType
- schedule, workHours, workFormat
- postedBy, isActive, isFeatured
- views, applications, expiresAt

## 🔐 Безопасность

- JWT токены для аутентификации
- Хеширование паролей (bcrypt)
- Rate limiting
- CORS настройки
- Helmet для безопасности заголовков
- Валидация входных данных

## 🧪 Тестирование

```bash
# Запуск тестов (когда будут добавлены)
npm test
```

## 📝 Логирование

Сервер логирует:
- Подключение к базе данных
- Ошибки
- Статус сервера

## 🚀 Деплой

1. **Соберите проект:**
```bash
npm run build
```

2. **Настройте переменные окружения для продакшна**

3. **Запустите:**
```bash
npm start
```

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории.
